<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Certbot - Create SSL</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #121212;
      color: #fff;
      font-family: 'Inter', sans-serif;
    }
    .container {
      max-width: 600px;
      margin-top: 60px;
    }
    label {
      margin-bottom: 0.5rem;
    }
  </style>
</head>
<body>
  <div class="container">
    <h3 class="text-center mb-4">🔒 Create SSL Certificate</h3>

    <% if (message) { %>
      <div class="alert alert-<%= type === 'error' ? 'danger' : 'success' %> text-center">
        <%= message %>
      </div>
    <% } %>

    <form action="/certbot" method="post">
      <div class="mb-3">
        <label for="domain" class="form-label">Domain Name</label>
        <input type="text" name="domain" class="form-control" placeholder="example.com" required>
      </div>
      <button type="submit" class="btn btn-primary w-100">🚀 Get Certificate</button>
    </form>

    <div class="text-center mt-4">
      <a href="/" class="btn btn-secondary">← Back to Panel</a>
    </div>

    <footer class="text-center mt-5 text-secondary">
      <hr>
      <small>© <%= new Date().getFullYear() %> Mehti</small>
    </footer>
  </div>
</body>
</html>
