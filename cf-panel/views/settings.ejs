<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Reverse Proxy Panel</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #121212;
      color: #fff;
      font-family: 'Inter', sans-serif;
    }
    .container {
      max-width: 600px;
    }
    .form-control, .btn {
      border-radius: 0.5rem;
    }
  </style>
</head>
<body>
  <div class="container mt-5">
    <h3 class="text-center mb-4">⚙️ Reverse Proxy Settings</h3>
    <form action="/save" method="POST">
      <div class="mb-3">
        <label for="target" class="form-label">Target URL (e.g. https://api.example.com)</label>
        <input type="text" name="target" id="target" class="form-control" value="<%= target %>" required>
      </div>
      <div class="mb-3">
        <label for="port" class="form-label">Port (e.g. 443)</label>
        <input type="text" name="port" id="port" class="form-control" value="<%= port %>" required>
      </div>
      <div class="text-center">
        <button class="btn btn-primary px-4">💾 Save Settings</button>
        <a href="/certbot" class="btn btn-outline-light ms-3">🔐 SSL Certbot</a>
      </div>
    </form>

    <footer class="text-center mt-5 text-secondary">
      <hr>
      <small>© <%= new Date().getFullYear() %> Mehti</small>
    </footer>
  </div>
</body>
</html>
